{% extends 'base.html.twig' %}

{% block title %}Editer un utilisateur{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 style="margin-top: 2em">Editer un utilisateur</h1>

        {{ include('back_office/users/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <a class="btn btn-dark" href="{{ path('users_index') }}">Retour</a>

        {{ include('back_office/users/_delete_form.html.twig') }}
    </div>
{% endblock %}

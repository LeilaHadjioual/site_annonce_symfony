{% extends 'base.html.twig' %}

{% block title %}Utilisateurs{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 style="margin-top: 2em">Utilisateurs</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>E-mail</th>
                <td>{{ user.email }}</td>
            </tr>
            {#            <tr>#}
            {#                <th>Password</th>#}
            {#                <td>{{ user.password }}</td>#}
            {#            </tr>#}
            <tr>
                <th>Téléphone</th>
                <td>{{ user.phone }}</td>
            </tr>
            <tr>
                <th>Rôle</th>
                <td>{{ user.role }}</td>
                </th>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-dark" href="{{ path('users_index') }}">Retour</a>

        <a class="btn btn-warning" href="{{ path('users_edit', {'id': user.id}) }}">Editer</a>


        {{ include('back_office/users/_delete_form.html.twig') }}
    </div>
{% endblock %}

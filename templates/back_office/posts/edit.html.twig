{% extends 'base.html.twig' %}

{% block title %}Editer une annonce{% endblock %}

{% block body %}
    <div class="container" >
        <div>
            <h1>Editer une annonce</h1>
            {{ include('back_office/posts/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div>
            <a href="{{ path('posts_index') }}">back to list</a>
        </div>
        <div>
            {{ include('back_office/posts/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Editer une annonce{% endblock %}

{% block body %}
    <div class="container mt-4 formContrainer">
        <div>
            <a class="btn btnBack" href="{{ path('posts_index') }}">Retour</a>
        </div>
        <h1 class="h1Style">Editer une annonce</h1>
        {{ include('back_office/posts/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        <div>
            {{ include('back_office/posts/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}

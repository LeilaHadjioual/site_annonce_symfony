{% extends 'base.html.twig' %}

{% block title %}Editer une annonce{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 style="margin-top: 2em">Editer une annonce</h1>
        {{ include('back_office/posts/_form.html.twig', {'button_label': 'Modifier'}) }}

        <div>
            <a class="btn btn-dark" href="{{ path('posts_index') }}">Retour</a>
        </div>
        <div>
            {{ include('back_office/posts/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
